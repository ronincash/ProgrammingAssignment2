## Put comments here that give an overall description of what your
## functions do
#####################################
#The following 2 functions work together to cache the inverse of a matrix
#This can be used to save time in certain situations.
#####################################
## Write a short comment describing this function
######################
#This function makes a special 'matrix' object that can cache its inverse.
#####################
makeCacheMatrix <- function(x = matrix()) {

}


## Write a short comment describing this function
#########################
#This function computes the inverse of the special 'matrix' returned by the function above.
#If the inverse has already been calculated, and the matrix has not changed,
#this function will use the cached result.
#########################
cacheSolve <- function(x, ...) {
        ## Return a matrix that is the inverse of 'x'
}
